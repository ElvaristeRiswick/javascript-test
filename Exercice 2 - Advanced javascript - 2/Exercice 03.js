/*⚠️ NE MODIFIEZ PAS LE NOM DES DÉCLARATIONS ⚠️*/
/*3️⃣ EXERCICE 3️⃣*/

//function areEqual(x, y) {
// Returns true if "x" and "y" are equal.
// Otherwise it returns false.
// Your code:
//}

//let x = 22;
//let y = 32;
//if (x == y) {
//console.log("true");
///} else {
//console.log("false");
//}

function HaveTheSameLength(str1, str2) {
  // Returns true if the two strings have the same length.
  // Otherwise it returns false.
  // Your code:
}

function lessThanNinety(num) {
  // Returns true if the argument "num" is less than ninety.
  // Otherwise it returns false.
  // Your code:
}
//let num = 90;
//if (num > 220) {
//console.log("true");
//}
//if(num < 220) {
//console.log("false");
//}

function greaterthanfifty(num) {
  // Returns true if the argument "num" is greater than fifty.
  // Otherwise it returns false.
  // Your code:
}

let num = 50;
if (num > 200) {
  console.log("true");
}
if (num < 200) {
  console.log("false");
}

function isEven(num) {
  // Returns true if "num" is even.
  // Otherwise it returns false.
  // Your code:
}

//let num = parseInt(prompt("inter an intergre  number  even"));
//let result = num % 2 == 0;
//console.log(`${result}`);

function isOdd(num) {
  // Returns true if "num" is odd.
  //Otherwise it returns false.
  //YOur code:
}

//let number = parseInt(prompt("inter an intergre number odd"));
//let result = number % 3 == 0;
//console.log(`${result}`);
